# extraction

Purpose:
- Convert `CleanedPost` inputs into `InterviewEvent` outputs.

Scope:
- Own the extraction workflow contract.
- Stay independent from storage, HTTP, and orchestration concerns.

Status:
- Scaffold only. Business logic is intentionally left as TODOs.
